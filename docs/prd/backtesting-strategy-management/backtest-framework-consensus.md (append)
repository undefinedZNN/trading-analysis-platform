## 6. 执行撮合层细化

### 6.1 核心职责
- 管理订单生命周期：接收策略指令（风控放行后）、生成内部订单对象、跟踪状态变化。
- 基于行情事件模拟撮合，处理市价、限价、止损、止盈等订单，并注入滑点、手续费、内部条件。
- 生成 `execution.report`、`portfolio.update`、`ledger.record` 等事件，反馈成交结果与仓位变化。
- 支持撤单、TIF（GTC/IOC/FOK）、内部止损触发，与事件总线联合实现暂停/恢复时的状态快照。

### 6.2 数据结构与订单生命周期

```typescript
type OrderType = 'market' | 'limit' | 'stop' | 'stop-limit';
type OrderStatus =
  | 'pending'
  | 'new'
  | 'partially_filled'
  | 'filled'
  | 'cancelled'
  | 'rejected'
  | 'expired';

interface OrderEntry {
  orderId: string;
  intentId: string;
  strategyId: string;
  symbol: string;
  side: 'buy' | 'sell';
  type: OrderType;
  tif: 'GTC' | 'IOC' | 'FOK';
  limitPrice?: string;
  stopPrice?: string;
  quantity: string;
  remaining: string;
  createdAt: string;
  status: OrderStatus;
  metadata?: Record<string, unknown>; // 内部止损/风控标记等
}

interface OrderBookState {
  activeOrders: Map<string, OrderEntry>;
  restingBids: OrderEntry[];    // 按价格/时间排序
  restingAsks: OrderEntry[];
  triggeredQueue: OrderEntry[]; // 等待执行的触发单
}
```

- 生命周期：
  1. `strategy.intent` → 创建 `OrderEntry`（status=`new`），发送初始 `execution.report`。
  2. 行情事件驱动撮合 → 更新 `remaining`、`status`，生成相应 `execution.report`。
  3. 处理撤单/过期 → 将状态置为 `cancelled`/`expired` 并产生回报。
  4. 订单完成后从 `activeOrders` 移除并记录到账簿。

### 6.3 撮合流程
- **市价单**：按当前 bar 的成交价格执行，可配置基准价（`open`/`close`/`mid`）并应用滑点。
- **限价单**：判断 `limitPrice` 是否在 bar 的高低范围内，触及时根据成交策略计算价格。
- **止损/止盈**：当 bar 触及 `stopPrice` 或内部阈值时，将订单转入 `triggeredQueue`，按市价或限价执行。
- **部分成交 & TIF**：
  - 未完全成交的订单在 `remaining > 0` 时保留；`IOC` 自动撤掉剩余；`FOK` 若无法一次到底则整体撤单。
- **内部条件**：支持内部止损（例如固定回撤）、时间到期等条件；触发时生成反向订单或直接输出平仓回报。

### 6.4 滑点与手续费模型

```typescript
interface SlippageModel {
  apply(input: {
    basePrice: string;
    side: 'buy' | 'sell';
    quantity: string;
    liquidity: 'maker' | 'taker';
  }): string;
}

interface FeeModel {
  compute(input: {
    price: string;
    quantity: string;
    side: 'buy' | 'sell';
    liquidity: 'maker' | 'taker';
  }): { amount: string; asset: string };
}
```

- 两个模型通过依赖注入提供，默认实现支持固定点差、按比例手续费。未来可扩展分层滑点、交易所费率等。
- 成交回报中写入 `fills.fee` 与 `fills.liquidity`，供账簿和绩效分析使用。

### 6.5 事件生成与仓位同步
- 每次成交/撤单都会发 `execution.report`，字段沿用第 4 章 schema。
- 撮合层同步计算仓位/余额变化，生成 `portfolio.update` 更新策略视角的账户状态。
- 对成交写入 `ledger.record`（含 `tradeId`、`intentId`、`pnl`、`fees`、`factors`、`sequenceId`）供结果分析和二次复盘。

### 6.6 撤单与异常处理
- 撤单指令结构：

```typescript
interface CancelInstruction {
  strategyId: string;
  intentId: string;
  reason?: string;
}
```

- 撮合层查找 `OrderEntry` 更新状态并广播 `execution.report`（status=`cancelled`）。
- 撮合过程中异常（如价格非法、模型报错）转化为 `DeadLetterEvent` 并将对应订单标记为 `rejected`，同时在日志中记录原因。

### 6.7 状态快照与恢复

```typescript
interface ExecutionSnapshot {
  orders: OrderEntry[];
  triggeredQueue: OrderEntry[];
  lastProcessedSeq: string;
}
```

- 与事件总线快照协同：暂停时序列化当前订单簿、触发队列和最近处理的事件序号；恢复时重建状态并从 `lastProcessedSeq` 之后继续撮合。
- 必要时对 `triggeredQueue` 立即重评估（防止重复触发或漏执行）。

### 6.8 一期简化策略
- 初期可采用简化规则以加快交付：
  - 市价单按 `bar.open` 成交，施加固定滑点；
  - 限价单只判断是否触及，不模拟订单簿排队；
  - 手续费固定费率，内部止损先支持简单阈值；
  - 不实现复杂队列优先级和成交深度模拟，但保留接口以便后续升级。
- 文档明确这些取舍，确保后续迭代能平滑切换到更真实的撮合模型。

如需调整或扩展以上共识，请继续补充。框架方案的进一步细化将以此为基线。

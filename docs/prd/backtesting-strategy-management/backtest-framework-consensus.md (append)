## 9. 交易账簿与日志层细化

### 9.1 核心职责
- 汇总事件总线中的执行、风控、策略日志与指标，形成结构化账簿与复盘数据。
- 按会话组织存储（Parquet+Arrow+JSON），为“交易结果分析”模块、可视化仪表盘和离线研究提供数据。
- 提供索引与查询接口，将交易与事件时间线、特征、风控记录关联，实现可追溯分析。
- 与快照/恢复流程协同，保证中断后数据的一致性与增量写入。

### 9.2 事件订阅与管道
- 订阅事件类型：
  - `execution.report`：成交、撤单、部分成交信息；
  - `portfolio.update`：账户余额、仓位、权益曲线；
  - `risk.decision`：风控判定、拒单、强平动作；
  - `ledger.record`：逐笔交易记录（撮合层产出）；
  - `strategy.log` / `strategy.metric`：策略日志与指标；
  - `control`：回测启动、暂停、快照、结束等控制事件。
- 管道流程：
  1. 接收事件→归一化→缓冲；
  2. 根据事件类型写入相应 ledger 表（批量写 Parquet）；
  3. 同步写入 JSONL 或 Arrow IPC 文件供实时查看；
  4. 更新内存索引（哈希/skip list）以支持快速查询。
- 支持实时推送（用于前端监控）与离线批处理（会话结束时落盘）。

### 9.3 数据模型

```typescript
interface TradeLedgerRow {
  sessionId: string;
  tradeId: string;
  intentId: string;
  fillId: string;
  strategyId: string;
  symbol: string;
  side: 'buy' | 'sell';
  quantity: string;
  price: string;
  pnl: string;
  fee?: string;
  liquidity?: 'maker' | 'taker';
  features?: Record<string, string | number>;
  sequenceId: string;
  timestamp: string;
}

interface OrderLedgerRow {
  sessionId: string;
  intentId: string;
  orderId: string;
  status: OrderStatus;
  quantity: string;
  filled: string;
  remaining: string;
  price?: string;
  tif?: string;
  reason?: string;
  sequenceId: string;
  timestamp: string;
}

interface AccountSnapshotRow {
  sessionId: string;
  timestamp: string;
  equity: string;
  cash: string;
  marginUsage: string;
  positions: Record<string, PositionSnapshot>;
  drawdown?: string;
}

interface RiskEventRow {
  sessionId: string;
  strategyId: string;
  intentId?: string;
  ruleId: string;
  decision: RiskDecisionType;
  reasonCode?: string;
  severity?: 'info' | 'warn' | 'error';
  followUp?: Record<string, unknown>;
  sequenceId: string;
  timestamp: string;
}

interface StrategyLogRow {
  sessionId: string;
  strategyId: string;
  level: 'debug' | 'info' | 'warn' | 'error';
  message: string;
  extra?: Record<string, unknown>;
  sequenceId: string;
  timestamp: string;
}
```

- 所有行包含 `sessionId`、`sequenceId`、`timestamp` 用于联查；必要时额外存 `runId` 或 `batchId`。
- `features` 字段引用 `feature_catalog.parquet`，存当时的特征快照，支持深度分析。

### 9.4 存储布局
- 会话目录结构（示例 `/backtests/{sessionId}/`）：
  - `trade_ledger.parquet`
  - `order_ledger.parquet`
  - `account_snapshots.parquet`
  - `risk_events.parquet`
  - `strategy_logs.jsonl` / `strategy_logs.parquet`
  - `strategy_metrics.parquet`
  - `feature_catalog.parquet` & `featureCatalog.json`
  - `event_timeline.parquet`（可选，存控制/风控关键事件）
- 写入策略：
  - 事件实时落入内存缓冲（Arrow Table），定时或定量 flush 成 Parquet；
  - 使用 DuckDB/Arrow 直接查询 Parquet，无需额外导入；
  - JSONL 用于快速预览（日志），也方便前端分页加载。

### 9.5 查询与接口
- 对外提供查询接口/工具：
  - `listTrades(filter)`：支持按 symbol、时间区间、PnL 区间筛选；
  - `getTradeContext(tradeId)`：返回交易关联的 `TradeLedgerRow`＋周边 `OrderLedgerRow`＋相关 `RiskEventRow`＋`StrategyLogRow`；
  - `getPnLCurve()`：从 `account_snapshots` 生成权益曲线；
  - `exportLogs(level, range)`：按级别与时间导出策略日志。
- 分析模块可直接使用 DuckDB SQL 查询 Parquet，或通过 REST/gRPC API 获取数据。
- 允许将结果导出为 CSV/JSON/Arrow，根据用户需求。

### 9.6 与快照/恢复协同
- 快照写入时记录 ledger 最新 `sequenceId` 与 flush 状态（是否还有未写磁盘的事件）。
- 恢复时，从 `eventStoreCheckpoint` 继续消费事件，同时确保 ledger 不重复写入（可根据 `sequenceId` 去重）。
- 对于进行中的会话，可选择“增量写 + 回放补齐”策略：先将未落盘的事件回放再继续正常运行。

### 9.7 一期实现范围
- 实现上述 Parquet 表结构，提供基础查询/导出功能；
- 与策略日志、指标事件打通，确保所有重要事件可追踪；
- 提供简单 CLI/脚本用于查看交易列表、PnL、风控记录；
- 后续可扩展高级分析（如交易标签、因子贡献、可视化面板）或实时订阅接口。

如需调整或扩展以上共识，请继续补充。框架方案的进一步细化将以此为基线。

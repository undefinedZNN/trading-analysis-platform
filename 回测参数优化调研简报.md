# 回测参数优化调研简报

**日期**: 2025-11-27  
**状态**: 调研完成，待讨论

---

## 🎯 核心发现

### 1. 当前系统问题

✅ **已识别的问题**:

1. **参数传递简化过度**
   - 前端发送 `makerFee` 和 `takerFee`，但后端传给Worker时简化为单一 `commission`
   - 丢失了Maker/Taker差异信息

2. **缺少资产类型区分**
   - 数据集表没有 `assetType` 字段
   - 无法区分股票、期货、加密货币
   - 无法存储合约规格（multiplier、tick size等）

3. **佣金模型单一**
   - Worker只支持百分比佣金
   - 不支持固定佣金（期货常见）
   - 不支持最低佣金限制（A股有5元最低佣金）

### 2. Backtrader佣金机制核心

Backtrader底层已经支持完善的佣金系统，只是我们没有充分利用：

```python
# 关键参数
cerebro.broker.setcommission(
    commission=0.001,      # 佣金值
    mult=1.0,              # 合约乘数（期货重要）
    margin=None,           # 保证金
    commtype=COMM_PERC,    # COMM_PERC（百分比）或 COMM_FIXED（固定）
    stocklike=True,        # True=股票模式, False=期货模式
    percabs=True           # True表示0.001=0.1%
)
```

**股票 vs 期货的关键差异**:

| 特性 | 股票 (`stocklike=True`) | 期货 (`stocklike=False`) |
|------|------------------------|-------------------------|
| 价值计算 | `value = size * price` | `value = size * price * mult` |
| 现金管理 | 买入扣全款，卖出收全款 | 占用保证金，每日结算浮盈亏 |
| 合约乘数 | 不需要（`mult=1`） | 必须设置（如E-mini S&P 500是50） |
| 佣金类型 | 通常百分比 | 固定或百分比都常见 |

---

## 🎨 优化方案概览

### 方案：渐进式优化（推荐）

**优点**: 向后兼容、分阶段实施、风险可控

### 核心改动

#### 1. 数据库扩展

```sql
-- 添加资产类型
ALTER TABLE datasets ADD COLUMN asset_type TEXT DEFAULT 'crypto';

-- 添加合约规格
ALTER TABLE datasets ADD COLUMN contract_specs JSONB;

-- 示例：期货合约规格
{
  "multiplier": 10,        -- 合约乘数（螺纹钢10吨/手）
  "tickSize": 1.0,         -- 最小变动价位
  "marginRatio": 0.09,     -- 保证金比例9%
  "lotSize": 1,            -- 最小交易单位
  "currency": "CNY"        -- 计价货币
}
```

#### 2. 后端DTO重构

**新增类型**:

```typescript
// 资产类型枚举
enum AssetType {
  STOCK = 'stock',      // 股票
  FUTURES = 'futures',  // 期货
  CRYPTO = 'crypto',    // 加密货币
  FOREX = 'forex'       // 外汇
}

// 佣金配置类型
enum CommissionType {
  PERCENTAGE = 'percentage',    // 百分比（股票常用）
  FIXED = 'fixed',              // 固定金额（期货常用）
  MAKER_TAKER = 'maker-taker',  // Maker/Taker差异（加密货币）
  TIERED = 'tiered'             // 分段佣金（VIP等级）
}

// 佣金配置DTO
class CommissionConfigDto {
  type: CommissionType;
  
  // 百分比模式
  rate?: number;              // 佣金率
  
  // 固定模式
  amount?: number;            // 固定金额
  
  // Maker/Taker模式
  makerRate?: number;
  takerRate?: number;
  
  // 可选
  minCommission?: number;     // 最低佣金
  stampDuty?: number;         // 印花税（A股）
}

// 合约规格DTO
class ContractSpecsDto {
  multiplier?: number;        // 合约乘数
  tickSize?: number;          // 最小变动价位
  lotSize?: number;           // 最小交易单位
  marginRatio?: number;       // 保证金比例
  currency?: string;          // 计价货币
}

// 执行配置DTO（重构）
class ExecutionConfigDto {
  initialCapital: number;
  assetType: AssetType;
  contractSpecs?: ContractSpecsDto;
  commission: CommissionConfigDto;
  slippage?: number;
  leverage?: number;
  
  // 保留旧字段（向后兼容）
  @deprecated
  fees?: { makerFee: number; takerFee: number };
}
```

#### 3. Worker实现

```python
# backtest-worker/src/backtrader_integration/commission/commission_manager.py

class CommissionManager:
    def setup_broker(self, cerebro, execution_config):
        """根据资产类型配置Broker"""
        asset_type = execution_config['assetType']
        
        if asset_type == 'stock':
            self._setup_stock_commission(cerebro, execution_config)
        elif asset_type == 'futures':
            self._setup_futures_commission(cerebro, execution_config)
        elif asset_type == 'crypto':
            self._setup_crypto_commission(cerebro, execution_config)
    
    def _setup_stock_commission(self, cerebro, config):
        """股票佣金配置"""
        comm_config = config['commission']
        
        if comm_config['type'] == 'percentage':
            # 支持最低佣金
            class StockCommInfo(bt.CommInfoBase):
                params = (
                    ('commission', comm_config['rate']),
                    ('min_commission', comm_config.get('minCommission', 0)),
                    ('stamp_duty', comm_config.get('stampDuty', 0)),
                    ('stocklike', True),
                )
                
                def _getcommission(self, size, price, pseudoexec):
                    comm = abs(size) * price * self.p.commission
                    comm = max(comm, self.p.min_commission)
                    
                    # 卖出时加印花税
                    if size < 0:
                        comm += abs(size) * price * self.p.stamp_duty
                    
                    return comm
            
            cerebro.broker.addcommissioninfo(StockCommInfo())
    
    def _setup_futures_commission(self, cerebro, config):
        """期货佣金配置"""
        specs = config['contractSpecs']
        comm_config = config['commission']
        
        if comm_config['type'] == 'fixed':
            # 固定佣金
            cerebro.broker.setcommission(
                commission=comm_config['amount'],
                mult=specs['multiplier'],
                automargin=specs['marginRatio'],
                stocklike=False,
                commtype=bt.CommInfoBase.COMM_FIXED
            )
        elif comm_config['type'] == 'percentage':
            # 百分比佣金
            cerebro.broker.setcommission(
                commission=comm_config['rate'],
                mult=specs['multiplier'],
                automargin=specs['marginRatio'],
                stocklike=False,
                commtype=bt.CommInfoBase.COMM_PERC,
                percabs=True
            )
```

---

## 📝 真实市场配置示例

### 中国A股

```json
{
  "assetType": "stock",
  "contractSpecs": {
    "lotSize": 100,
    "tickSize": 0.01,
    "currency": "CNY"
  },
  "commission": {
    "type": "percentage",
    "rate": 0.0003,
    "minCommission": 5.0,
    "stampDuty": 0.001
  }
}
```

**说明**:
- 佣金率：万分之3（`0.0003`）
- 最低佣金：5元
- 印花税：千分之1（仅卖出时收取）
- 最小手数：100股

### 螺纹钢期货（rb2401）

```json
{
  "assetType": "futures",
  "contractSpecs": {
    "multiplier": 10,
    "tickSize": 1.0,
    "marginRatio": 0.09,
    "currency": "CNY"
  },
  "commission": {
    "type": "percentage",
    "rate": 0.0001
  }
}
```

**说明**:
- 合约乘数：10吨/手
- 保证金比例：9%
- 佣金率：万分之1
- 最小变动：1元/吨

### 币安现货

```json
{
  "assetType": "crypto",
  "commission": {
    "type": "maker-taker",
    "makerRate": 0.001,
    "takerRate": 0.001
  }
}
```

**说明**:
- Maker费率：0.1%
- Taker费率：0.1%
- 无合约乘数
- 按实际成交价值计算

---

## 🗓 实施计划

### Phase 1: 数据模型扩展（2-3天）
- [ ] 数据库迁移：添加 `asset_type` 和 `contract_specs` 字段
- [ ] Entity 更新
- [ ] 向后兼容处理（现有数据设置为 `crypto`）

### Phase 2: DTO和API扩展（3-4天）
- [ ] 创建新的类型定义（`AssetType`, `CommissionType`等）
- [ ] 重构 `ExecutionConfigDto`
- [ ] 保留旧字段并添加兼容性转换
- [ ] API文档更新

### Phase 3: Worker实现（4-5天）
- [ ] 创建 `CommissionManager` 类
- [ ] 实现股票佣金处理
- [ ] 实现期货佣金处理
- [ ] 实现加密货币佣金处理
- [ ] 单元测试

### Phase 4: 前端适配（3-4天）
- [ ] 创建任务时增加资产类型选择
- [ ] 根据资产类型显示不同配置项
- [ ] 动态表单验证
- [ ] UI/UX优化

### Phase 5: 测试和文档（2-3天）
- [ ] 集成测试
- [ ] 端到端测试
- [ ] 用户文档
- [ ] 开发文档
- [ ] 示例策略

**总工期**: 14-19个工作日

---

## 🚨 注意事项

### 向后兼容策略

```typescript
// 自动转换旧格式
function migrateExecutionConfig(oldConfig: any): ExecutionConfigDto {
  if (oldConfig.fees && !oldConfig.commission) {
    return {
      ...oldConfig,
      assetType: 'crypto',
      commission: {
        type: 'maker-taker',
        makerRate: oldConfig.fees.makerFee,
        takerRate: oldConfig.fees.takerFee
      }
    };
  }
  return oldConfig;
}
```

### 优先级建议

#### P0 - 必须实现
- ✅ 资产类型枚举
- ✅ 百分比 vs 固定佣金
- ✅ 期货合约乘数
- ✅ 期货保证金比例

#### P1 - 重要但可延后
- 🔶 Maker/Taker精确区分（当前用平均值近似）
- 🔶 最低佣金限制
- 🔶 印花税支持

#### P2 - 可选
- 🔷 分段佣金（VIP等级）
- 🔷 Tick size限制
- 🔷 Lot size限制

---

## 💬 待讨论问题

### 1. 资产类型优先级

**问题**: 应该先支持哪种资产类型？

**选项**:
- A. 先做期货（因为差异最大，参数最复杂）
- B. 先做股票（因为用户可能更熟悉）
- C. 先做加密货币（当前默认类型）

**建议**: 选择A，因为期货是最复杂的场景，做完期货后其他类型会更简单。

### 2. 向后兼容程度

**问题**: 是否完全向后兼容？

**选项**:
- A. 完全兼容：旧API继续可用，自动转换
- B. 弃用警告：旧API可用但返回警告，3个月后移除
- C. 破坏性更新：直接移除旧API

**建议**: 选择A，因为系统还在早期，用户不多，但保持兼容性是良好实践。

### 3. Maker/Taker处理

**问题**: Backtrader原生不支持Maker/Taker区分，如何处理？

**选项**:
- A. 使用平均费率（简单但不精确）
- B. 自定义CommInfo（复杂但精确，需要hook订单类型）
- C. 等待Backtrader更新（时间不确定）

**建议**: 
- Phase 1 使用选项A（快速上线）
- Phase 2 实现选项B（长期优化）

### 4. 数据集迁移

**问题**: 现有数据集如何设置资产类型？

**选项**:
- A. 全部默认为 `crypto`
- B. 根据 `tradingPair` 智能识别（如包含"USDT"则为crypto）
- C. 手动标注

**建议**: 选择A + C组合，默认crypto，然后提供批量编辑界面让用户修正。

---

## 📚 参考资料

### 详细调研报告
完整技术细节请查看：
📄 `docs/architecture/backtrader-migration/COMMISSION_ASSET_TYPE_RESEARCH.md`

### 相关代码位置
- 前端DTO: `frontend/src/modules/backtesting/components/CreateBacktestTaskModal.tsx`
- 后端DTO: `backend/src/backtesting/tasks/dto/create-backtest-task.dto.ts`
- RabbitMQ消息: `backend/src/backtesting/tasks/rabbitmq-task-dispatcher.service.ts`
- Worker执行器: `backtest-worker/src/backtrader_integration/execution/backtest_executor.py`
- Backtrader佣金: `backtrader/comminfo.py`, `backtrader/commissions/__init__.py`

---

## ✅ 下一步

1. **确认需求优先级**
   - 确定先支持哪种资产类型
   - 确定必须实现的功能（P0）

2. **选择实施方案**
   - 渐进式优化 vs 一次性重构
   - 确定时间表

3. **开始Phase 1**
   - 数据库迁移
   - Entity更新
   - 基础测试

**期待您的反馈！** 🚀

